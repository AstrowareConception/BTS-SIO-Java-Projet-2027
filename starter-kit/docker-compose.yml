services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: media_starter
      POSTGRES_USER: media
      POSTGRES_PASSWORD: media
    ports:
      - "5432:5432"
    volumes:
      - media_pgdata:/var/lib/postgresql/data

volumes:
  media_pgdata:
